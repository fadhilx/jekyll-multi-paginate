require "spec_helper"

RSpec.describe Jekyll::Multi::Paginate do
  it "has a version number" do
    expect(Jekyll::Multi::Paginate::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
